---
title: "Duckdb"
description: "Use Duckdb with Kern agents."
---
```python

Enable Kern agents to integrate with DuckDB for fast analytical queries (OLAP) on structured data.

from kern.agent import Agent
from kern.tools.duckdb import DuckDbTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------


agent = Agent(
    tools=[DuckDbTools()],
    instructions="Use this file for Movies data: https://kern-public.s3.amazonaws.com/demo_data/IMDB-Movie-Data.csv",
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    agent.print_response(
        "What is the average rating of movies?", markdown=True, stream=False
    )
```

## Run the Example
```bash
# Clone and setup repo
git clone https://github.com/kern-ai/kern.git
cd kern/cookbook/91_tools

# Create and activate virtual environment
./scripts/demo_setup.sh
source .venvs/demo/bin/activate

python duckdb_tools.py
```
For details, see [DuckDB cookbook](https://github.com/kern-ai/kern/blob/main/cookbook/91_tools/duckdb_tools.py).