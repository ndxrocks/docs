---
title: "Resend"
description: "Use Resend tools with Kern agents."
---

Enable Kern agents to move beyond the chat interface and interact with users directly in their inbox for sending emails, batch delivery, scheduling and status tracking.

```python

from kern.agent import Agent
from kern.tools.resend import ResendTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

from_email = "<enter_from_email>"
to_email = "<enter_to_email>"

agent = Agent(tools=[ResendTools(from_email=from_email)])

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    agent.print_response(f"Send an email to {to_email} greeting them with hello world")
```

## Run the Example
```bash
# Clone and setup repo
git clone https://github.com/kern-ai/kern.git
cd kern/cookbook/91_tools

# Create and activate virtual environment
./scripts/demo_setup.sh
source .venvs/demo/bin/activate

python resend_tools.py
```
For details, see [Resend cookbook](https://github.com/kern-ai/kern/blob/main/cookbook/91_tools/resend_tools.py)