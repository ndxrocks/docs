---
title: Agent with Tools
description: "Give your agent tools to interact with external services."
mode: wide
---

Give an agent tools to interact with external services. The agent uses `HackerNewsTools` to fetch trending stories and user details.

<Steps>

  <Step title="Create a Python file">
    ```python tools.py
    from kern.agent import Agent
    from kern.models.anthropic import Claude
    from kern.tools.hackernews import HackerNewsTools

    agent = Agent(
        model=Claude(id="claude-sonnet-4-5"),
        tools=[HackerNewsTools()],
        instructions="Write a report on the topic.",
        markdown=True,
    )

    agent.print_response("Trending AI startups on Hacker News", stream=True)
    ```
  </Step>

  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U kern-ai anthropic
    ```
  </Step>

  <Step title="Export your Anthropic API key">

    <CodeGroup>

    ```bash Mac/Linux
    export ANTHROPIC_API_KEY="your_anthropic_api_key_here"
    ```

    ```bash Windows
    $Env:ANTHROPIC_API_KEY="your_anthropic_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run Agent">
    ```bash
    python tools.py
    ```
  </Step>

</Steps>
