---
title: PgVector Async
---

## Code

```python cookbook/08_knowledge/vector_db/pgvector/async_pg_vector.py
import asyncio

from kern.agent import Agent
from kern.knowledge.knowledge import Knowledge
from kern.vectordb.pgvector import PgVector

db_url = "postgresql+psycopg://ai:ai@localhost:5532/ai"

vector_db = PgVector(table_name="recipes", db_url=db_url)

knowledge_base = Knowledge(
    vector_db=vector_db,
)

agent = Agent(knowledge=knowledge_base)

if __name__ == "__main__":

    asyncio.run(
        knowledge_base.ainsert(url="https://kern.ndx.rocks/introduction/agents.md")
    )

    asyncio.run(
        agent.aprint_response("What is the purpose of an Kern Agent?", markdown=True)
    )
```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U psycopg2-binary pgvector pypdf openai kern-ai
    ```
  </Step>
  <Snippet file="run-pgvector-docker.mdx" />
  <Step title="Set environment variables">
    ```bash
    export OPENAI_API_KEY=xxx
    ```
  </Step>
  <Step title="Run Agent">
    ```bash
    python cookbook/08_knowledge/vector_db/pgvector/async_pg_vector.py
    ```
  </Step>
</Steps>