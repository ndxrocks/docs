---
title: Huggingface Embedder
---

## Code

```python
from kern.knowledge.embedder.huggingface import HuggingfaceCustomEmbedder
from kern.knowledge.knowledge import Knowledge
from kern.vectordb.pgvector import PgVector

embeddings = HuggingfaceCustomEmbedder().get_embedding(
    "The quick brown fox jumps over the lazy dog."
)

# Print the embeddings and their dimensions
print(f"Embeddings: {embeddings[:5]}")
print(f"Dimensions: {len(embeddings)}")

# Example usage:
knowledge = Knowledge(
    vector_db=PgVector(
        db_url="postgresql+psycopg://ai:ai@localhost:5532/ai",
        table_name="huggingface_embeddings",
        embedder=HuggingfaceCustomEmbedder(),
    ),
    max_results=2,
)

```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Set your API key">
    ```bash
    export HUGGINGFACE_API_KEY=xxx
    ```
  </Step>

  <Step title="Install dependencies">
    ```bash
    uv pip install -U sqlalchemy psycopg pgvector huggingface-hub kern-ai
    ```
  </Step>

  <Snippet file="run-pgvector-step.mdx" />

  <Step title="Run Agent">
    <CodeGroup>
    ```bash Mac
    python cookbook/08_knowledge/embedders/huggingface_embedder.py
    ```

    ```bash Windows
    python cookbook/08_knowledge/embedders/huggingface_embedder.py
    ```
    </CodeGroup>
  </Step>
</Steps>