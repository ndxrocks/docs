---
title: Embedders
description: 29 embedding providers including OpenAI, Cohere, HuggingFace, and local options.
---

Embedders convert text into vectors for similarity search. Kern supports 29 embedding providers with a consistent interface.

```python
from kern.knowledge.embedder.openai import OpenAIEmbedder
from kern.knowledge.knowledge import Knowledge
from kern.vectordb.pgvector import PgVector

knowledge = Knowledge(
    vector_db=PgVector(
        table_name="docs",
        db_url="postgresql://...",
        embedder=OpenAIEmbedder(model="text-embedding-3-small"),
    ),
)
```

## Supported Embedders

| Provider | Import | Models |
|----------|--------|--------|
| OpenAI | `from kern.knowledge.embedder.openai import OpenAIEmbedder` | text-embedding-3-small, text-embedding-3-large |
| Azure OpenAI | `from kern.knowledge.embedder.azure import AzureOpenAIEmbedder` | text-embedding-3-small via Azure |
| Cohere | `from kern.knowledge.embedder.cohere import CohereEmbedder` | embed-english-v3.0, embed-multilingual-v3.0 |
| Google | `from kern.knowledge.embedder.gemini import GeminiEmbedder` | text-embedding-004 |
| Mistral | `from kern.knowledge.embedder.mistral import MistralEmbedder` | mistral-embed |
| HuggingFace | `from kern.knowledge.embedder.huggingface import HuggingFaceEmbedder` | Any sentence-transformer |
| Ollama | `from kern.knowledge.embedder.ollama import OllamaEmbedder` | nomic-embed-text, mxbai-embed-large |
| Jina | `from kern.knowledge.embedder.jina import JinaEmbedder` | jina-embeddings-v3 |
| Fireworks | `from kern.knowledge.embedder.fireworks import FireworksEmbedder` | nomic-embed-text-v1.5 |
| AWS Bedrock | `from kern.knowledge.embedder.aws import AWSBedrockEmbedder` | Titan embeddings |
| Voyage | `from kern.knowledge.embedder.voyage import VoyageEmbedder` | voyage-2, voyage-code-2 |

## Examples by Provider

### OpenAI

The most common choice for production systems.

```python cookbook/07_knowledge/embedders/openai_embedder.py
from kern.knowledge.embedder.openai import OpenAIEmbedder
from kern.knowledge.knowledge import Knowledge
from kern.vectordb.pgvector import PgVector

embedder = OpenAIEmbedder(model="text-embedding-3-small")

# Get embeddings directly
embeddings = embedder.get_embedding("Hello, world!")
print(f"Dimensions: {len(embeddings)}")  # 1536

# Use in knowledge base
knowledge = Knowledge(
    vector_db=PgVector(
        table_name="docs",
        db_url="postgresql://...",
        embedder=embedder,
    ),
)
```

### OpenAI with Batching

Efficient embedding for large document sets.

```python cookbook/07_knowledge/embedders/openai_embedder_batching.py
from kern.knowledge.embedder.openai import OpenAIEmbedder

embedder = OpenAIEmbedder(
    model="text-embedding-3-small",
    batch_size=100,  # Process 100 texts at a time
)
```

### Cohere

Strong multilingual support.

```python cookbook/07_knowledge/embedders/cohere_embedder.py
from kern.knowledge.embedder.cohere import CohereEmbedder

embedder = CohereEmbedder(
    model="embed-english-v3.0",
    input_type="search_document",
)
```

### Google Gemini

Google's embedding models.

```python cookbook/07_knowledge/embedders/gemini_embedder.py
from kern.knowledge.embedder.gemini import GeminiEmbedder

embedder = GeminiEmbedder(model="text-embedding-004")
```

### Azure OpenAI

OpenAI embeddings through Azure.

```python cookbook/07_knowledge/embedders/azure_embedder.py
from kern.knowledge.embedder.azure import AzureOpenAIEmbedder

embedder = AzureOpenAIEmbedder(
    model="text-embedding-3-small",
    azure_endpoint="https://your-resource.openai.azure.com/",
    api_version="2024-02-01",
)
```

### HuggingFace

Use any sentence-transformer model locally.

```python cookbook/07_knowledge/embedders/huggingface_embedder.py
from kern.knowledge.embedder.huggingface import HuggingFaceEmbedder

embedder = HuggingFaceEmbedder(
    model="sentence-transformers/all-MiniLM-L6-v2",
)
```

### Ollama

Local embeddings with Ollama.

```python cookbook/07_knowledge/embedders/ollama_embedder.py
from kern.knowledge.embedder.ollama import OllamaEmbedder

embedder = OllamaEmbedder(
    model="nomic-embed-text",
    host="http://localhost:11434",
)
```

### Mistral

Mistral's embedding model.

```python cookbook/07_knowledge/embedders/mistral_embedder.py
from kern.knowledge.embedder.mistral import MistralEmbedder

embedder = MistralEmbedder(model="mistral-embed")
```

### Jina

Specialized embeddings for various tasks.

```python cookbook/07_knowledge/embedders/jina_embedder.py
from kern.knowledge.embedder.jina import JinaEmbedder

embedder = JinaEmbedder(model="jina-embeddings-v3")
```

### AWS Bedrock

Embeddings through AWS Bedrock.

```python cookbook/07_knowledge/embedders/aws_bedrock_embedder.py
from kern.knowledge.embedder.aws import AWSBedrockEmbedder

embedder = AWSBedrockEmbedder(
    model="amazon.titan-embed-text-v1",
    region="us-east-1",
)
```

### Fireworks

Fast inference for open-source embedding models.

```python cookbook/07_knowledge/embedders/fireworks_embedder.py
from kern.knowledge.embedder.fireworks import FireworksEmbedder

embedder = FireworksEmbedder(
    model="nomic-ai/nomic-embed-text-v1.5",
)
```

## Run Examples

```bash
git clone https://github.com/kern-ai/kern.git
cd kern/cookbook/07_knowledge/embedders

export OPENAI_API_KEY=xxx

# OpenAI
python openai_embedder.py

# HuggingFace (local)
python huggingface_embedder.py

# Ollama (local)
python ollama_embedder.py
```

