---
title: Together
description: Use Llama, Mistral, Qwen, and other open-source models via Together.
---

Together AI provides access to a wide range of open-source models including Llama, Mistral, and Qwen.

```python
from kern.agent import Agent
from kern.models.together import Together

agent = Agent(
    model=Together(id="meta-llama/Meta-Llama-3.1-70B-Instruct-Turbo"),
    markdown=True,
)

agent.print_response("Explain the CAP theorem", stream=True)
```

## Tool Use

```python
from kern.agent import Agent
from kern.models.together import Together
from kern.tools.yfinance import YFinanceTools

agent = Agent(
    model=Together(id="meta-llama/Meta-Llama-3.1-70B-Instruct-Turbo"),
    tools=[YFinanceTools(stock_price=True)],
    markdown=True,
)

agent.print_response("What's TSLA's stock price?", stream=True)
```

## Structured Output

```python
from pydantic import BaseModel, Field
from kern.agent import Agent
from kern.models.together import Together

class Analysis(BaseModel):
    summary: str = Field(..., description="Brief summary")
    sentiment: str = Field(..., description="Sentiment")

agent = Agent(
    model=Together(id="meta-llama/Meta-Llama-3.1-70B-Instruct-Turbo"),
    output_schema=Analysis,
)

agent.print_response("Analyze the future of AI")
```

## Run Examples

```bash
export TOGETHER_API_KEY=xxx

git clone https://github.com/kern-ai/kern.git
cd kern/cookbook/90_models/together

python basic.py
python tool_use.py
python structured_output.py
```
