---
title: Performance with Teams
description: Example showing how to analyze the runtime and memory usage of an Kern Team.
---



<Steps>

  <Step title="Create a Python file">
    ```python performance_team_instantiation.py
    """Run `uv pip install kern-ai openai` to install dependencies."""

    from kern.agent import Agent
    from kern.eval.performance import PerformanceEval
    from kern.models.openai import OpenAIResponses
    from kern.team.team import Team

    team_member = Agent(model=OpenAIResponses(id="gpt-5.2"))


    def instantiate_team():
        return Team(members=[team_member])


    instantiation_perf = PerformanceEval(
        name="Instantiation Performance Team", func=instantiate_team, num_iterations=1000
    )

    if __name__ == "__main__":
        instantiation_perf.run(print_results=True, print_summary=True)
    ```
  </Step>

  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U openai kern-ai
    ```
  </Step>

  <Step title="Export your OpenAI API key">

    <CodeGroup>

    ```bash Mac/Linux
      export OPENAI_API_KEY="your_openai_api_key_here"
    ```

    ```bash Windows
      $Env:OPENAI_API_KEY="your_openai_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run Team">
    ```bash
    python performance_team_instantiation.py
    ```
  </Step>

</Steps>
