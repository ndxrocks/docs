---
title: Image File Input Agent
---

## Code

```python cookbook/11_models/mistral/image_file_input_agent.py
from pathlib import Path

from kern.agent import Agent
from kern.media import Image
from kern.models.mistral.mistral import MistralChat
from kern.tools.hackernews import HackerNewsTools

agent = Agent(
    model=MistralChat(id="pixtral-12b-2409"),
    tools=[
        HackerNewsTools()
    ],  # pixtral-12b-2409 is not so great at tool calls, but it might work.
    markdown=True,
)

image_path = Path(__file__).parent.joinpath("sample.jpeg")

agent.print_response(
    "Tell me about this image and give me the latest news about it from HackerNews.",
    images=[
        Image(filepath=image_path),
    ],
    stream=True,
)

```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Set your API key">
    ```bash
    export MISTRAL_API_KEY=xxx
    ```
  </Step>

  <Step title="Install dependencies">
    ```bash
    uv pip install -U mistralai kern-ai
    ```
  </Step>

  <Step title="Run Agent">
    ```bash
    python cookbook/11_models/mistral/image_file_input_agent.py
    ```
  </Step>
</Steps>
