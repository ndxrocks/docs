---
title: Async Streaming Agent
---

## Code

```python cookbook/11_models/siliconflow/async_basic_stream.py
import asyncio

from kern.agent import Agent
from kern.models.siliconflow import Siliconflow

agent = Agent(model=Siliconflow(id="openai/gpt-oss-120b"), markdown=True)

asyncio.run(agent.aprint_response("Share a 2 sentence horror story", stream=True))

```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

<Step title="Set your API key">```bash export SILICONFLOW_API_KEY=xxx ```</Step>

<Step title="Install dependencies">```bash uv pip install -U kern-ai openai ```</Step>

  <Step title="Run Agent">
    ```bash
    python cookbook/11_models/siliconflow/async_basic_stream.py
    ```
  </Step>
</Steps>
