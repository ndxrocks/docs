---
title: Async Tool Use
---

## Code

```python cookbook/11_models/litellm/async_tool_use.py
import asyncio

from kern.agent import Agent
from kern.models.litellm import LiteLLM
from kern.tools.hackernews import HackerNewsTools

agent = Agent(
    model=LiteLLM(
        id="gpt-5-mini",
        name="LiteLLM",
    ),
    markdown=True,
    tools=[HackerNewsTools()],
)

# Ask a question that would likely trigger tool use
asyncio.run(agent.aprint_response("What is happening in France?"))

```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Set your API key">
    ```bash
    export LITELLM_API_KEY=xxx
    ```
  </Step>

  <Step title="Install dependencies">
    ```bash
    uv pip install -U litellm kern-ai
    ```
  </Step>

  <Step title="Run Agent">
    ```bash
    python cookbook/11_models/litellm/async_tool_use.py
    ```
  </Step>
</Steps>
