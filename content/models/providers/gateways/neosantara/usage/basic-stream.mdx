---
title: Basic Stream
---

## Code

```python cookbook/90_models/neosantara/basic_stream.py
from kern.agent import Agent
from kern.models.neosantara import Neosantara

agent = Agent(model=Neosantara(id="grok-4.1-fast-non-reasoning"), markdown=True)

# Print the response in the terminal
agent.print_response("Share a 2 sentence horror story", stream=True)
```

## Usage

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Set your API key">
    ```bash
    export NEOSANTARA_API_KEY=xxx
    ```
  </Step>

  <Step title="Install dependencies">
    ```bash
    uv pip install -U openai kern-ai
    ```
  </Step>

  <Step title="Run Agent">
    ```bash
    python cookbook/90_models/neosantara/basic_stream.py
    ```
  </Step>
</Steps>
