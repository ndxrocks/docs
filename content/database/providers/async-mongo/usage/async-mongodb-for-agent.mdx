---
title: Async MongoDB for Agent
sidebarTitle: Agent
---

Kern supports using MongoDB asynchronously as a storage backend for Agents, with the `AsyncMongoDb` class.

## Usage

You need to provide either `db_url` or `client`. The following example uses `db_url`.

### Run MongoDB
Install [docker desktop](https://docs.docker.com/desktop/install/mac-install/) and run **MongoDB** on port **27017** using:
```bash
docker run -d \
  --name local-mongo \
  -p 27017:27017 \
  -e MONGO_INITDB_ROOT_USERNAME=mongoadmin \
  -e MONGO_INITDB_ROOT_PASSWORD=secret \
  mongo
```

```python async_mongodb_for_agent.py
"""
Run `uv pip install kern-ai openai motor pymongo` to install dependencies.
"""
from kern.agent import Agent
from kern.db.mongo import AsyncMongoDb
from kern.tools.hackernews import HackerNewsTools

# MongoDB connection settings
db_url = "mongodb://localhost:27017"

db = AsyncMongoDb(db_url=db_url)

agent = Agent(
    db=db,
    tools=[HackerNewsTools()],
    add_history_to_context=True,
)
agent.print_response("How many people live in Canada?")
agent.print_response("What is their national anthem called?")
```

## Params

<Snippet file="db-async-mongodb-params.mdx" />