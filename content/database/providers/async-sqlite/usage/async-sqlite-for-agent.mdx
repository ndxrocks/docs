---
title: Async Sqlite for Agent
sidebarTitle: Agent
---

Kern supports using Sqlite asynchronously as a storage backend for Agents, with the `AsyncSqliteDb` class.

## Usage

```python async_sqlite_for_agent.py
"""
Run `uv pip install openai sqlalchemy aiosqlite` to install dependencies.
"""
import asyncio

from kern.agent import Agent
from kern.db.sqlite import AsyncSqliteDb
from kern.tools.hackernews import HackerNewsTools

# Initialize AsyncSqliteDb
db = AsyncSqliteDb(db_file="agent_storage.db")

agent = Agent(
    db=db,
    tools=[HackerNewsTools()],
    add_history_to_context=True,
    add_datetime_to_context=True,
)

asyncio.run(agent.aprint_response("How many people live in Canada?"))
asyncio.run(agent.aprint_response("What is their national anthem called?"))

```

## Params

<Snippet file="db-async-sqlite-params.mdx" />