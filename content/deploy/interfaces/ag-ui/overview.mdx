---
title: AG-UI
sidebarTitle: AG-UI
description: Expose agents via the AG-UI protocol for frontend integration.
keywords: [ag-ui, deploy, interface, frontend, protocol]
---

The AG-UI interface exposes any Kern agent or team via the [AG-UI protocol](https://docs.ag-ui.com/), enabling real-time streaming to compatible frontends like Dojo and CopilotKit.

## Quick Start

```python agui_agent.py
from kern.agent import Agent
from kern.models.openai import OpenAIResponses
from kern.os.app import AgentOS
from kern.os.interfaces.agui import AGUI

agent = Agent(name="Assistant", model=OpenAIResponses(id="gpt-5.4"))

agent_os = AgentOS(
    agents=[agent],
    interfaces=[AGUI(agent=agent)],
)
app = agent_os.get_app()
```

```bash
uv pip install 'kern-ai[agui]'
python -m kern.os.serve agui_agent:app --port 9001
```

## How It Works

| Concept | Behavior |
| --- | --- |
| **Protocol** | AG-UI standard for agent ↔ frontend communication |
| **Streaming** | Real-time token streaming with tool call visibility |
| **Custom Events** | Stream structured data (charts, profiles) to frontend |
| **Frontends** | Works with Dojo, CopilotKit, or any AG-UI client |

## Setup

No external configuration required. Run the agent and connect your frontend to the `/agui` endpoint.

**Endpoint:** `http://localhost:9001/agui`

## Next Steps

<CardGroup cols={2}>
  <Card title="Full Guide" icon="book" href="/agent-os/interfaces/ag-ui/introduction">
    Custom events, state sync, and frontend integration.
  </Card>
  <Card title="Examples" icon="code" href="/agent-os/usage/interfaces/ag-ui/basic">
    Basic agent, tools, and team examples.
  </Card>
  <Card title="Cookbook" icon="github" href="https://github.com/kern-ai/kern/tree/main/cookbook/05_agent_os/interfaces/agui">
    Full runnable examples on GitHub.
  </Card>
  <Card title="AG-UI Protocol" icon="file-code" href="https://docs.ag-ui.com/">
    Official protocol specification.
  </Card>
</CardGroup>
